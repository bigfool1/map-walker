# map-walker 数据库配置示例
# 复制此文件为 .env 即可生效：cp .env.example .env

# 数据库驱动: sqlite | mysql
DB_DRIVER=mysql

# MySQL DSN: user:pass@tcp(host:port)/dbname
DB_DSN=root:@tcp(127.0.0.1:3306)/map_walker

# SQLite 默认（不设置环境变量时）：
# DB_DRIVER=sqlite
# DB_DSN=data/map-walker.db
